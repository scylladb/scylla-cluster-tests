/** jobDescription

    Artifacts test for Rocky Linux 9 with SELinux enabled on GCE.
    Validates Scylla installation, configuration, and basic functionality on Rocky Linux 9 with SELinux enforcing mode.
    Verifies package integrity and SELinux policy compatibility.

    This test is part of the artifacts suite.

    Labels: artifacts, rocky-9, gce, selinux, security, installation
*/

#! groovy

// trick from https://github.com/jenkinsci/workflow-cps-global-lib-plugin/pull/43
def lib = library identifier: 'sct@snapshot', retriever: legacySCM(scm)

artifactsPipeline(
    test_config: 'test-cases/artifacts/rocky9-selinux.yaml',
    backend: 'gce',
    provision_type: 'spot',

    timeout: [time: 30, unit: 'MINUTES'],
    post_behavior_db_nodes: 'destroy'
)
