#!groovy

/** jobDescription
    Latency performance regression test on 3-node cluster.
        Stress tool: cassandra-stress.
        Runs for 5 minutes.
        Tests lightweight transactions (LWT).
        Part of the performance suite.
        Labels: latency, lwt, performance
*/

// trick from https://github.com/jenkinsci/workflow-cps-global-lib-plugin/pull/43
def lib = library identifier: 'sct@snapshot', retriever: legacySCM(scm)

perfRegressionParallelPipeline(
    backend: 'aws',
    region: 'eu-west-1',
    test_name: 'performance_regression_lwt_test.PerformanceRegressionLWTTest',
    test_config: 'test-cases/performance/perf-regression-latency-lwt-small.yaml',
    sub_tests: ["test_latency"],
    timeout: [time: 300, unit: 'MINUTES']
)
