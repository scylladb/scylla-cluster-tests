test_duration: 420

n_db_nodes: 6
simulated_racks: 3
n_loaders: 2
round_robin: true

instance_type_db: 'i7i.2xlarge'
gce_instance_type_db: 'n2-highmem-8'
gce_n_local_ssd_disk_db: 8

nemesis_class_name: ['SisyphusMonkey']
nemesis_selector: ["schema_changes and not disruptive"]
nemesis_seed: 21
nemesis_during_prepare: false
space_node_threshold: 64424

user_prefix: 'longevity-lwt-24h'

use_preinstalled_scylla: true

# Required for TABLET_SPLIT/MERGE event detection in DB logs
append_scylla_args: '--logger-log-level table=debug'

latte_schema_parameters:
  lwt: 'true'
  cdc: 'false'
  mv: 'false'
  ks_name: 'lwt_keyspace'
  table_name: 'lwt_table'

prepare_write_cmd:
  - >-
    latte run -f write_to_table --tag write-1
    -P rows_per_partition=1000
    --rate=2500 --duration=5000000 --start-cycle=0
    data_dir/latte/tablet_split_merge_load.rn

  - >-
    latte run -f write_to_table --tag write-2
    -P rows_per_partition=1000
    --rate=2500 --duration=5000000 --start-cycle=5000001
    data_dir/latte/tablet_split_merge_load.rn

stress_cmd:
  - >-
    latte run -f update_first_part_of_table --serial-consistency=LOCAL_SERIAL --tag update-first-part
    --threads=1 --rate=500 --concurrency=900 --duration=360m
    data_dir/latte/tablet_split_merge_load.rn

  - >-
    latte run -f update_second_part_of_table --serial-consistency=LOCAL_SERIAL --tag update-second-part
    --threads=1 --rate=500 --concurrency=900 --duration=360m
    data_dir/latte/tablet_split_merge_load.rn

  - >-
    latte run -f read_from_table --consistency=LOCAL_SERIAL --tag read-from-table
    --threads=1 --rate=800 --concurrency=900 --duration=360m
    data_dir/latte/tablet_split_merge_load.rn

  - >-
    latte run -f delete_partition --tag delete-partition
    -P delete_pct=50
    --rate=20 --concurrency=10 --duration=360m
    data_dir/latte/tablet_split_merge_load.rn
